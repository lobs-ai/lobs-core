- **[2026-05-11] [finding]** — Strategic reflection findings: (1) lobs-memory wasn't running — launchd plist had malformed ProgramArguments syntax. Fixed plist, rebuilt dist, restarted. (2) Query expansion showed "not implemented" — expander.ts existed but was never imported/initialized AND health endpoint hardcoded loaded:false. Fixed by wiring initExpander() into search.ts, adding isExpanderEnabled(), updating health endpoint. Verified: expansionMs=1118 on complex queries. (3) Sentinel "stale tasks" warning was false — UI showed old failed attempts but tasks table was clean. (4) Commit: bfd89a5.
